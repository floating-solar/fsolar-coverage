services:
  - type: web
    name: dem-coverage-analyzer
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 180
    envVars:
      - key: OPENTOPO_API_KEY
        sync: false
